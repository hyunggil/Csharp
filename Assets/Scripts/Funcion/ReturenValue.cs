using UnityEngine;
//반환값이 잇는 함수 만들고 호출하기
public class ReturenValue : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //[2]반환갑이 있는 함수 호출(사용)
        //반환값을 받아줄 변수 선언하고 바환 되는 값 저장
        
    }
    //[1]반환갑이 있는 함수 만들기(선언, 정의)
    //반환값 이라는 문자열(string)을 반환 하는 함수
    void GetString()
    {
        
    }
}
