using UnityEngine;

public class CharacterDemo : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        char Eng = 'A';
        char Kor = '한';
        Debug.Log("영어는 " + Eng + "한글은 " + Kor);
    }


}
