using UnityEngine;

// Quates(따옴표): "", ''
public class Querts : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("[1] \"큰따옴표 출력");
        Debug.Log("[2] \'작은따옴표 출력 \n줄바꿈");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
