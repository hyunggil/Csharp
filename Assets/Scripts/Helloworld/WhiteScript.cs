using UnityEngine;
// WhiteSpace(공백) : 공백은 프로그램 무시, 가독성을 높일수있다.
public class WhiteScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("C#");
        Debug. Log ( "C#" );
        Debug
            .Log(
            "C#");
    }

   
}
